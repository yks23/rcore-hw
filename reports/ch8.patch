diff --git a/os/src/syscall/process.rs b/os/src/syscall/process.rs
index 01c5499..4aab131 100644
--- a/os/src/syscall/process.rs
+++ b/os/src/syscall/process.rs
@@ -151,12 +151,15 @@ pub fn sys_kill(pid: usize, signal: u32) -> isize {
 /// YOUR JOB: get time with second and microsecond
 /// HINT: You might reimplement it with virtual memory management.
 /// HINT: What if [`TimeVal`] is splitted by two pages ?
-pub fn sys_get_time(_ts: *mut TimeVal, _tz: usize) -> isize {
-    trace!(
-        "kernel:pid[{}] sys_get_time NOT IMPLEMENTED",
-        current_task().unwrap().process.upgrade().unwrap().getpid()
-    );
-    -1
+/// get time of day
+/// get time of day
+pub fn sys_get_time(ts: *mut TimeVal, _tz: usize) -> isize {
+    let us = crate::timer::get_time_us();
+    let token = current_user_token();
+    let tv = translated_refmut(token, ts);
+    tv.sec = us / 1_000_000;
+    tv.usec = us % 1_000_000;
+    0
 }
 
 /// mmap syscall
diff --git a/os/src/syscall/sync.rs b/os/src/syscall/sync.rs
index bd3e5e3..d1c538d 100644
--- a/os/src/syscall/sync.rs
+++ b/os/src/syscall/sync.rs
@@ -5,18 +5,50 @@ use alloc::sync::Arc;
 use alloc::vec;
 use alloc::vec::Vec;
 
+/// Check if ALL holders of resource `res_sem` are blocked (waiting for something).
+/// If any holder is free (not waiting and not the requesting thread), return false.
+fn all_holders_blocked(
+    sem_alloc: &[Vec<usize>],
+    sem_waiting: &[Option<usize>],
+    res_sem: usize,
+    tid: usize,
+) -> bool {
+    let num_threads = sem_alloc.len();
+    for t in 0..num_threads {
+        if t == tid {
+            continue;
+        }
+        if res_sem < sem_alloc[t].len() && sem_alloc[t][res_sem] > 0 {
+            let waiting = if t < sem_waiting.len() {
+                sem_waiting[t]
+            } else {
+                None
+            };
+            if waiting.is_none() {
+                return false;
+            }
+        }
+    }
+    true
+}
+
+/// Detect deadlock using wait-for graph with DFS.
+/// Only considers a cycle as deadlock if all holders along the path are blocked.
 fn detect_semaphore_deadlock(
     sem_alloc: &[Vec<usize>],
     sem_waiting: &[Option<usize>],
     tid: usize,
     sem_id: usize,
 ) -> bool {
-    let num_threads = sem_alloc.len();
+    if !all_holders_blocked(sem_alloc, sem_waiting, sem_id, tid) {
+        return false;
+    }
+    let num_threads = sem_alloc.len().max(if tid < sem_waiting.len() { sem_waiting.len() } else { tid + 1 });
     let mut visited = vec![false; num_threads];
     visited[tid] = true;
     let mut stack: Vec<usize> = Vec::new();
-    for t in 0..num_threads {
-        if sem_id < sem_alloc[t].len() && sem_alloc[t][sem_id] > 0 {
+    for t in 0..sem_alloc.len() {
+        if sem_id < sem_alloc[t].len() && sem_alloc[t][sem_id] > 0 && t != tid {
             stack.push(t);
         }
     }
@@ -24,17 +56,25 @@ fn detect_semaphore_deadlock(
         if holder == tid {
             return true;
         }
-        if visited[holder] {
+        if holder >= num_threads || visited[holder] {
             continue;
         }
         visited[holder] = true;
-        if let Some(wait_sem) = sem_waiting[holder] {
-            for t in 0..num_threads {
-                if wait_sem < sem_alloc[t].len() && sem_alloc[t][wait_sem] > 0 {
+        let wait_sem = if holder < sem_waiting.len() {
+            sem_waiting[holder]
+        } else {
+            None
+        };
+        if let Some(ws) = wait_sem {
+            if !all_holders_blocked(sem_alloc, sem_waiting, ws, tid) {
+                continue;
+            }
+            for t in 0..sem_alloc.len() {
+                if ws < sem_alloc[t].len() && sem_alloc[t][ws] > 0 {
                     if t == tid {
                         return true;
                     }
-                    if !visited[t] {
+                    if t < num_threads && !visited[t] {
                         stack.push(t);
                     }
                 }
diff --git a/os/src/task/mod.rs b/os/src/task/mod.rs
index 81a25be..9d8b3b7 100644
--- a/os/src/task/mod.rs
+++ b/os/src/task/mod.rs
@@ -41,8 +41,10 @@ pub use task::{TaskControlBlock, TaskStatus};
 
 /// Make current task suspended and switch to the next task
 pub fn suspend_current_and_run_next() {
-    // There must be an application running.
-    let task = take_current_task().unwrap();
+    let task = match take_current_task() {
+        Some(t) => t,
+        None => return,
+    };
 
     // ---- access current TCB exclusively
     let mut task_inner = task.inner_exclusive_access();
diff --git a/os/src/task/process.rs b/os/src/task/process.rs
index c2be1ce..2e21917 100644
--- a/os/src/task/process.rs
+++ b/os/src/task/process.rs
@@ -49,6 +49,14 @@ pub struct ProcessControlBlockInner {
     pub semaphore_list: Vec<Option<Arc<Semaphore>>>,
     /// condvar list
     pub condvar_list: Vec<Option<Arc<Condvar>>>,
+    /// deadlock detection enabled
+    pub deadlock_detect: bool,
+    /// which tid holds each mutex (usize::MAX = no holder)
+    pub mutex_holders: Vec<usize>,
+    /// allocation matrix: sem_alloc[tid][sem_id] = count held
+    pub sem_alloc: Vec<Vec<usize>>,
+    /// which semaphore each thread is waiting on (None = not waiting)
+    pub sem_waiting: Vec<Option<usize>>,
 }
 
 impl ProcessControlBlockInner {
@@ -119,6 +127,10 @@ impl ProcessControlBlock {
                     mutex_list: Vec::new(),
                     semaphore_list: Vec::new(),
                     condvar_list: Vec::new(),
+                    deadlock_detect: false,
+                    mutex_holders: Vec::new(),
+                    sem_alloc: Vec::new(),
+                    sem_waiting: Vec::new(),
                 })
             },
         });
@@ -245,6 +257,10 @@ impl ProcessControlBlock {
                     mutex_list: Vec::new(),
                     semaphore_list: Vec::new(),
                     condvar_list: Vec::new(),
+                    deadlock_detect: false,
+                    mutex_holders: Vec::new(),
+                    sem_alloc: Vec::new(),
+                    sem_waiting: Vec::new(),
                 })
             },
         });
diff --git a/os/src/task/processor.rs b/os/src/task/processor.rs
index b54f9c2..0fca1b6 100644
--- a/os/src/task/processor.rs
+++ b/os/src/task/processor.rs
@@ -52,6 +52,7 @@ lazy_static! {
 ///Loop `fetch_task` to get the process that needs to run, and switch the process through `__switch`
 pub fn run_tasks() {
     loop {
+        crate::timer::check_timer_no_trace();
         let mut processor = PROCESSOR.exclusive_access();
         if let Some(task) = fetch_task() {
             let idle_task_cx_ptr = processor.get_idle_task_cx_ptr();
@@ -69,7 +70,8 @@ pub fn run_tasks() {
                 __switch(idle_task_cx_ptr, next_task_cx_ptr);
             }
         } else {
-            warn!("no tasks available in run_tasks");
+            drop(processor);
+            crate::timer::check_timer_no_trace();
         }
     }
 }
diff --git a/os/src/timer.rs b/os/src/timer.rs
index 6c1df2b..302d86c 100644
--- a/os/src/timer.rs
+++ b/os/src/timer.rs
@@ -98,6 +98,20 @@ pub fn remove_timer(task: Arc<TaskControlBlock>) {
     trace!("kernel: remove_timer END");
 }
 
+/// Check timers without requiring a current task (safe for idle loop)
+pub fn check_timer_no_trace() {
+    let current_ms = get_time_ms();
+    let mut timers = TIMERS.exclusive_access();
+    while let Some(timer) = timers.peek() {
+        if timer.expire_ms <= current_ms {
+            wakeup_task(Arc::clone(&timer.task));
+            timers.pop();
+        } else {
+            break;
+        }
+    }
+}
+
 /// Check if the timer has expired
 pub fn check_timer() {
     trace!(
